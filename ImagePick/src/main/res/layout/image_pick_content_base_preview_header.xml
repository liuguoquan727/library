<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/header"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="top"
    android:background="#ffffff"
    >
  <RelativeLayout
      android:id="@+id/header_left"
      android:layout_width="54dp"
      android:layout_height="match_parent"
      android:gravity="center"
      style="@style/Toolbar.Button.Navigation"
      >
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/image_pick_ic_back"
        />
  </RelativeLayout>
  <TextView
      android:id="@+id/imagePosition"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:textColor="@color/image_pick_main_color_normal"
      android:textSize="18sp"
      />
  <RelativeLayout
      android:id="@+id/header_right"
      android:layout_width="wrap_content"
      android:layout_height="match_parent"
      android:layout_gravity="right"
      android:gravity="center"
      android:paddingLeft="16dp"
      android:paddingRight="16dp"
      style="@style/Toolbar.Button.Navigation"
      >
    <CheckBox
        android:id="@+id/selected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#00000000"
        android:button="@drawable/image_pick_selector_pick"
        android:checked="true"
        android:gravity="center"
        />
  </RelativeLayout>
</FrameLayout>